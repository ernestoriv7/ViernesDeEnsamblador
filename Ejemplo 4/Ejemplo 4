.data
B: .word 0:10

.text
	li a0, 0 # i = 0
	li t0, 10
	la s1, B # t0 = dir B[0]
	li a3, 100 # x = 100
	ciclo:
		sw a3, 0(s1) # B[i} = a3
		addi s1, s1, 4 # Siguiente posición de B
		addi a0, a0, 1 # i = i + 1
		addi a3, a3, 100 # x = x + 100
		bne a0, t0, ciclo
end:
	j end
		
		
		
		
		
